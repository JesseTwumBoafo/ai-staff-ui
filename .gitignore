# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-single
dist-ssr
*.local

# Build artefacts (electron-builder output)
release

# Secrets and local environment (the app stores provider keys via the OS
# keychain in userData, not a .env; these lines are belt-and-braces only)
.env
.env.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
